# איך לערוך את האתר

## שינוי טקסטים

פתח את הקובץ:

`app/content.ts`

כל הטקסטים שמופיעים באתר נמצאים בתוך `content`. צריך לשנות רק את הטקסט שנמצא בין המרכאות, ולשמור על שמות השדות ועל המרכאות עצמן.

לדוגמה:

```ts
mainTitle: "חצי שנה שמח, אהובה שלי",
```

## החלפת תמונות

1. צור תיקייה בשם `photos` בתוך `public`.
2. הכנס אליה את התמונות בשמות פשוטים, לדוגמה `01.jpg`, `02.jpg`.
3. בתוך `app/content.ts`, החלף את הכתובות ברשימה `photos`.

לדוגמה:

```ts
export const photos = [
  "/photos/01.jpg",
  "/photos/02.jpg",
];
```

שלוש התמונות האחרונות ברשימה מופיעות במסך הסיום. הראשונה מביניהן היא התמונה המרכזית.

## הפעלה במחשב

נדרשת גרסת Node.js עדכנית.

```bash
npm install
npm run dev
```

אחרי שינוי, אפשר לשלוח לי את הקובץ `content.ts` או את תיקיית האתר המעודכנת ואפרסם את הגרסה החדשה באותו קישור.
